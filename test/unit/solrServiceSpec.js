/**
 * This file is subject to the terms and conditions defined in the
 * 'LICENSE.txt' file, which is part of this source code package.
 */
'use strict';

/*
describe('the Solr module', function() {

    // @see http://docs.angularjs.org/api/angular.module
    // try revising the module to use the conventional service declaration
    // and then create a new service for generating the default query
    //
    // configure existing services inside initialization blocks.
    // myModule.config(function($locationProvider) {
    // Configure existing providers
    //    $locationProvider.hashPrefix('!');
    //});

    // var injector = angular.injector(['ng','Solr']);
    // var service = injector.get('SolrSearchService',[]);

    // var module = angular.module('Solr', ['$rootScope','$http','$log']);
    // console.log(module);

    var injector = angular.injector(['$rootScope','$http','$log','Solr']);
    console.log(injector);

    var service = injector.get('SolrSearchService',['$rootScope','$http','$log']);
    console.log(service);

    it('should return a SolrSearchService service instance', function() {
        expect(service).not.toBe(null);
    });

    describe('the SolrSearchService instance', function() {

        it('should have atleast 10 functions declared', function() {
            var count = 0;
            for (var key in service) {
                count++;
            }
            expect(count).toBeGreaterThan(9);
        });

    });

});
*/
